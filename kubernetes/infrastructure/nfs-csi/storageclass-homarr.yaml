apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-homarr
  annotations:
    storageclass.kubernetes.io/is-default-class: "false"
provisioner: nfs.csi.k8s.io
parameters:
  server: ${NAS_IP}
  share: ${NAS_DATA_PATH}/homarr
reclaimPolicy: Retain
volumeBindingMode: Immediate
mountOptions:
  - nfsvers=3
  - nolock
  - async
  - hard
